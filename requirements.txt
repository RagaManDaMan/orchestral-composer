# Core audio processing
librosa>=0.10.2
soundfile>=0.12.0

# MIDI construction
mido>=1.3.0
pretty_midi>=0.2.9

# LLM communication
requests>=2.28.0

# Claude API (optional — used for reharmonization when local model output is rated bad)
# Set ANTHROPIC_API_KEY env var before using the "Improve with Claude API" button
anthropic>=0.104.0

# UI
gradio>=6.0.0

# Scientific computing (usually present; listed for completeness)
numpy>=1.24.0
scipy>=1.11.0

noisereduce>=2.0.0

# High-quality pitch shifting for Indian sample engine (requires Rubber Band library)
pyrubberband>=0.3.0
